// Fill these in from Firebase Console > Project settings > General > Your apps > Web app > SDK setup.
// These values are NOT secret — they're meant to be public in client code. Security comes from
// Firestore security rules (see the setup notes), not from hiding this config.
window.REP_LOG_FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
